version: '3'

services:
  app_gp2f5act16:
    image: app_gp2f5act16:2.2
    expose:
      - "80"

  nginx:
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app_gp2f5act16
    ports:
      - "80:80"
